export namespace Model {
    export namespace Account {
        export interface AccountModel {
            username : string;
            email : string;
            rank : number;
            emailVerified : boolean;
        }
    }
}